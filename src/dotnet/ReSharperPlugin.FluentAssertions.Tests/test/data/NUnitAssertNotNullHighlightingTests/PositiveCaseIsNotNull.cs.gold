using NUnit.Framework;

public class ExampleTests
{
    [Test]
    public void Test()
    {
        // arrange

        // act
        string result = null;
        
        // assert
        |Assert.IsNotNull(result)|(0);
    }
}
---------------------------------------------------------
(0): ReSharper Warning: (T) 'NUnit assertion can be replaced with FluentAssertion equivalents' (E) 'Replace 'Assert.IsNotNull(result)' with 'result.Should().BeNotNull()''
